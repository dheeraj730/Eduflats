{% extends baseLayout %}

{% block title %}Register website Form{% endblock %}

{% block form %}
{{ form_start(form, {attr:{novalidate: 'novalidate'}}) }}
<table>
   
     <tr>
        <td align="left">{{form_label(form.name)}}</td>
        <td>{{form_widget(form.name)}}</td>
        <td>{{form_errors(form.name)}}</td>
    </tr>
    <tr>
        <td align="left">{{form_label(form.address)}}</td>
        <td>{{form_widget(form.address)}}</td>
        <td>{{form_errors(form.address)}}</td>
    </tr>
    <tr>
        <td></td>
        <td>{{form_widget(form.submit)}}</td>
    </tr>
</table>
{{form_end(form)}}
{% endblock %}